# Pull request status (2026-05-24)

Aggiornamento dopo `main` @ **`72fa15b`** (`62e25d5` R2–R4, `db72dce`, `876bcd2`, `f851b61`, Watch control strategy). **Nessun merge automatico.**

## PR #8 — Watch experimental → `main`

| Campo | Valore |
|-------|--------|
| URL | https://github.com/egopfe/DirDiving-App/pull/8 |
| Head | `codex/experimental-features` |
| Base | `main` |
| Stato (`gh`, 2026-05-24 post-push) | `mergeable: UNKNOWN`; check `build` fallito; **non safe-to-merge** |
| Raccomandazione | **Review manuale — non auto-merge** |

**Cambiamenti tipici:** `SnorkelingView`, `ApneaView`, `ExplorationStore`, `project.yml` (inclusione sperimentali).

**Rischi:** promozione accidentale Snorkeling/Apnea/Buddy nel target MAIN; regressione unità sync, banner risalita inline, tombstone `dirdiving_shared_deleted_session_ids`, security F1–F12, readiness R2–R4 e strategia controlli Watch su `main`.

**Check manuali:** `xcodegen generate`; build Watch; `project.yml` excludes; BUSSOLA; GPS surface-only; preservare Diving live.

## PR #9 — iOS experimental → `main-iOS`

| Campo | Valore |
|-------|--------|
| URL | https://github.com/egopfe/DirDiving-App/pull/9 |
| Head | `codex/ios-experimental-features` |
| Base | `main-iOS` |
| Stato (`gh`, 2026-05-24) | `mergeable: CONFLICTING`; check `build` fallito; **non safe-to-merge** |
| Raccomandazione | **Review manuale — non auto-merge** |

**Rischi:** export iOS senza `.completeFileProtection`; bound CSV import meno restrittivi (Appendix A audit security).

**Check manuali:** ripristinare F4/F5 da `main`; build iOS; non promuovere Explore Lab a stable.

## Git consigliato

```bash
git fetch origin
# NON senza review:
# git checkout main && git merge origin/codex/experimental-features
```

---

*Verificato con `gh pr list` post-push: #8 risulta `mergeable: UNKNOWN`, #9 risulta `mergeable: CONFLICTING`, e il check GitHub Actions `build` è fallito su entrambe. Non mergeare senza risoluzione conflitti/mergeability aggiornata, build macOS e review manuale.*
