# Watch Activity Settings Access

- Global `SettingsView` remains on vertical `TabView` page `.settings`.
- `WatchInModeSettingsAccessButton` on Apnea/Snorkeling headers when `!isSessionActive`.
- `ContentView` blocks navigation to Settings during active sessions (live/compass only).
