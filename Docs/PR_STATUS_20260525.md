# DIR DIVING — PR Status 2026-05-25

**Date:** 2026-05-25  
**Inspection basis:** local repository refs and existing repo documentation  
**Important limitation:** `gh` is not installed in the current environment, and direct GitHub PR pages were not accessible here. This file is therefore a conservative branch/merge-risk summary, not a live API snapshot.

---

## A. PR visibility status

The repository history and prior reports consistently reference:

- PR #8 — `codex/experimental-features` -> `main`
- PR #9 — `codex/ios-experimental-features` -> `main-iOS`

No newer live PR metadata could be fetched from this environment.

---

## B. Branch evidence used

- `git branch -a`
- `git branch -vv`
- local `origin/*` refs
- divergence counts vs `origin/main`
- prior repo reports:
  - `PR_STATUS_20260524.md`
  - `PR_STATUS_20260523.md`
  - `PR_STATUS_20260520_POST_V9.md`

---

## C. Conservative current interpretation

### PR #8 — `codex/experimental-features` -> `main`

| Item | Status |
|------|--------|
| Branch type | Experimental Apple Watch |
| Runtime risk | High |
| Documentation risk | Medium |
| Merge recommendation | **Do not auto-merge** |
| Why | Experimental Snorkeling / Apnea / Buddy flows must remain isolated from MAIN runtime targets |
| Required QA before any merge | macOS build, target-membership review, UX review, safety/legal review, hardware validation |

### PR #9 — `codex/ios-experimental-features` -> `main-iOS`

| Item | Status |
|------|--------|
| Branch type | Experimental iOS companion |
| Runtime risk | High |
| Documentation risk | Medium |
| Merge recommendation | **Do not auto-merge** |
| Why | Experimental iOS surfaces remain isolated; `main-iOS` itself is now treated as a historical divergent worktree, not a stable MAIN baseline |
| Required QA before any merge | macOS build, manual diff review, sync/security regression check, planner/release review |

---

## D. Merge safety summary

At the documentation level, both referenced PRs should still be treated as:

- **unsafe for automatic merge**
- requiring manual conflict resolution
- requiring explicit protection of:
  - Diving MAIN stability
  - BUSSOLA terminology
  - inline underwater warning UX
  - compact GPS overlay behavior
  - legal/release/TestFlight docs
  - experimental isolation in `project.yml`

---

## E. Recommended next step

If live PR refresh is required, rerun inspection from an environment with:

- `gh` installed and authenticated, or
- direct access to the private GitHub PR pages/API

Until then, keep the previous standing recommendation: **do not auto-merge PR #8 or PR #9**.
