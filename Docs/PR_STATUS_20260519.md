# DIR DIVING — PR Status 2026-05-19

**Date:** 2026-05-19  
**Inspection basis:** `gh pr list`, `gh pr view`, local `origin/*` refs, baseline `main` @ `92e639a`

---

## A. Open PRs

| PR | Title | Head | Base | Opened | Files (approx) |
|----|-------|------|------|--------|----------------|
| [#8](https://github.com/egopfe/DirDiving-App/pull/8) | Update experimental Apnea workflow | `codex/experimental-features` | `main` | 2026-05-16 | 60+ (Watch experimental runtime + docs) |
| [#9](https://github.com/egopfe/DirDiving-App/pull/9) | Add experimental Apnea companion review | `codex/ios-experimental-features` | `main-iOS` | 2026-05-16 | 80+ (iOS experimental runtime + docs) |

---

## B. Divergence vs `origin/main`

| Branch ref | `origin/main` ahead | Branch ahead |
|-----------|----------------------|--------------|
| `origin/main` | 0 | 0 |
| `origin/main-iOS` | 216 | 48 |
| `origin/codex/experimental-features` | 80 | 29 |
| `origin/codex/ios-experimental-features` | 127 | 60 |

---

## C. PR #8 — `codex/experimental-features` → `main`

| Item | Assessment |
|------|------------|
| Branch type | Experimental Apple Watch |
| Affected area | Apnea workflow UI, Snorkeling views, Buddy Assist, legal onboarding on experimental branch, large doc matrix changes |
| Runtime risk | **High** — modifies Watch views/services excluded from MAIN target but PR base is `main` |
| Documentation risk | **Medium** — CSV/README may conflict with unified `main` narrative |
| Merge recommendation | **Do not auto-merge** |
| Required QA | macOS build both schemes; verify `project.yml` excludes experimental files from MAIN; UX/safety review; no Diving regression on `main` target |

**Notes from PR body:** claims depth/ascent algorithms unchanged; uses `ExplorationStore` for Apnea lifecycle wiring. Still inappropriate for direct merge to stable `main` without isolation review.

---

## D. PR #9 — `codex/ios-experimental-features` → `main-iOS`

| Item | Assessment |
|------|------------|
| Branch type | Experimental iOS companion |
| Affected area | Apnea Review card, exploration models/stores, massive file additions vs old `main-iOS` base |
| Runtime risk | **High** |
| Documentation risk | **Medium** |
| Merge recommendation | **Do not auto-merge** |
| Required QA | macOS build; sync/security regression; planner isolation; confirm base branch strategy (`main-iOS` is historical divergent worktree) |

**Notes from PR body:** explicit TODO placeholder wiring for Watch sync; no persistence contract changes claimed.

---

## E. Merge safety summary

Both PRs must be treated as:

- **unsafe for automatic merge**
- requiring manual conflict resolution on macOS
- requiring explicit protection of:
  - Diving MAIN stability on `main`
  - experimental isolation in `project.yml`
  - BUSSOLA terminology
  - inline underwater warning UX
  - compact GPS overlays
  - legal/release/TestFlight docs @ `92e639a`
  - Watch algorithm hardening assumptions

---

## F. Recommended actions

1. Keep PR #8 and #9 open until experimental features are intentionally promoted with dedicated review.
2. Rebase/re-target PR #9 discussion toward unified `main` workspace policy if iOS companion work continues on `main` instead of `main-iOS`.
3. After any future merge attempt, run full device QA checklists in `Docs/TESTFLIGHT_ENTITLEMENT_AND_DEVICE_QA_20260523.md` and `Docs/WATCH_IOS_SYNC_DEVICE_QA_CHECKLIST.md`.

**Standing recommendation:** do not auto-merge PR #8 or PR #9.
