# Pull request status (2026-05-20)

Aggiornamento documentale — **nessun merge automatico** eseguito.

## PR #8 — Watch experimental → `main`

| Campo | Valore |
|-------|--------|
| Titolo | Update experimental Apnea workflow |
| Head | `codex/experimental-features` |
| Base | `main` |
| File changed | ~40 |
| Commits | 26 |
| Merge recommendation | **Manual review — do not auto-merge** |

**Rischi:** sovrapposizione con `Services/WatchSyncService.swift`, `WatchDiveSyncCodec.swift`; import accidentale di `SnorkelingView`, `ApneaView`, `BuddyAssistView` nel target MAIN (`project.yml` li esclude).

**Checklist pre-merge:** build Watch MAIN; verificare `project.yml` excludes; preservare F1–F12; preservare banner risalita inline e tombstone `dirdiving_shared_deleted_session_ids` su `main`.

## PR #9 — iOS experimental → `main-iOS`

| Campo | Valore |
|-------|--------|
| Titolo | Add experimental Apnea companion review |
| Head | `codex/ios-experimental-features` |
| Base | `main-iOS` |
| File changed | ~95 |
| Commits | 116 |
| Merge recommendation | **Manual review — do not auto-merge** |

**Rischi:** regressioni security documentate — rimozione `.completeFileProtection`/cleanup export iOS; bound import CSV meno restrittivi vs `main` (vedi `Docs/SECURITY_AUDIT_MAIN_AND_MAIN_IOS_20260519.md` Appendix A).

**Checklist pre-merge:** ripristinare F4/F5 da `main` prima di unificare; build iOS; disclaimer planner invariato.

## Azione consigliata su GitHub

1. Commentare su ciascuna PR con link a questo file e a `Docs/DOCUMENTATION_UPDATE_REPORT_20260520_POST_RELEASE.md`.
2. Marcare **draft** o richiedere rebase su `main` / `main-iOS` aggiornati.
3. Merge solo dopo risoluzione conflitti su macOS e QA BUSSOLA/GPS surface-only.

```bash
# Esempio locale (non eseguito dall'agent)
git fetch origin
git checkout main && git merge origin/codex/experimental-features  # solo se review OK
```

---

*Stato API GitHub: mergeable unknown senza token CI; verificare in GitHub UI.*
