# Pull request status (2026-05-23)

Aggiornamento documentale dopo commit `5e595ee` su `main`. **Nessun merge automatico eseguito.**

## PR #8 — Watch experimental → `main`

| Campo | Valore |
|-------|--------|
| URL | https://github.com/egopfe/DirDiving-App/pull/8 |
| Head | `codex/experimental-features` |
| Base | `main` |
| Stato (202605-22) | `CONFLICTING` / `DIRTY` |
| Merge recommendation | **Manual review — do not auto-merge** |

**File tipici toccati:** `Views/SnorkelingView.swift`, `Views/ApneaView.swift`, `Services/WatchSyncService.swift`, `project.yml`.

**Rischi:** inclusione accidentale view experimental nel target MAIN; regressione banner risalita inline, tombstone `dirdiving_shared_deleted_session_ids`, security F1–F12.

**Checklist pre-merge:** build Watch; verificare `project.yml` excludes; preservare BUSSOLA (non COMPASSO); GPS surface-only; Snorkeling Live / Waypoint / Return map UI su branch experimental only.

## PR #9 — iOS experimental → `main-iOS`

| Campo | Valore |
|-------|--------|
| URL | https://github.com/egopfe/DirDiving-App/pull/9 |
| Head | `codex/ios-experimental-features` |
| Base | `main-iOS` |
| Stato (202605-22) | `CONFLICTING` / `DIRTY` |
| Merge recommendation | **Manual review — do not auto-merge** |

**Rischi:** rimozione `.completeFileProtection` export iOS; bound import CSV meno restrittivi (Appendix A security audit).

**Checklist pre-merge:** ripristinare F4/F5 da `main`; build iOS; planner disclaimer; non promuovere Explore Lab a stable.

## Azione Git consigliata

```bash
git fetch origin
# NON eseguire senza review:
# git checkout main && git merge origin/codex/experimental-features
```

Commentare sulle PR con link a questo file e a [`DOCUMENTATION_UPDATE_REPORT_20260523.md`](DOCUMENTATION_UPDATE_REPORT_20260523.md).

---

*Verificare stato mergeable aggiornato in GitHub UI (CLI `gh` non disponibile in ambiente agent).*
