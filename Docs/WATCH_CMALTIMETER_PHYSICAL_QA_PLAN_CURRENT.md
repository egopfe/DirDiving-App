# Watch CMAltimeter Physical QA Plan (Current)

**Status:** PENDING_PHYSICAL — do not claim PASS from simulator evidence.

## Required hardware

Supported Apple Watch with absolute altitude API (`CMAltimeter.isAbsoluteAltitudeAvailable()`).

## Scenarios

See `Docs/WATCH_CMALTIMETER_PHYSICAL_QA_MATRIX_CURRENT.csv` and `Docs/QA_EVIDENCE/WATCH_CMALTIMETER_PHYSICAL/`.

## Evidence collection

Use `EVIDENCE_TEMPLATE.md` per scenario. All rows remain `PENDING_PHYSICAL_QA` until real Watch execution.
