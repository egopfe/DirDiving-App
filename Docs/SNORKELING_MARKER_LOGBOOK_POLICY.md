# Snorkeling Marker Logbook Policy

- iOS logbook detail shows marker **category counts** and per-marker rows.
- Each row includes timestamp, category, GPS quality, and distance from entry when stored on the marker.
- Demo/fake sessions use existing demo badge; real stats exclude demo contamination.
- Map pins remain optional; P2 uses premium textual/card presentation.
