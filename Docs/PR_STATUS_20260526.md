# DIR DIVING — PR Status 2026-05-26

**Date:** 2026-05-26  
**Inspection basis:** local repository refs, fetched `origin/*` refs, and existing repo documentation  
**Important limitation:** `gh` is not installed in the current environment, so this file is a conservative branch/merge-risk summary rather than a live GitHub API snapshot.

---

## A. PR visibility status

The repository history and prior reports still reference:

- PR #8 — `codex/experimental-features` -> `main`
- PR #9 — `codex/ios-experimental-features` -> `main-iOS`

No newer live PR metadata could be fetched directly from GitHub in this environment.

---

## B. Branch evidence used

- `git fetch origin --prune`
- `git branch -a`
- `git branch -vv`
- divergence counts vs `origin/main`
- prior repo reports:
  - `PR_STATUS_20260525.md`
  - `PR_STATUS_20260524.md`
  - `PR_STATUS_20260520_POST_V9.md`

Current divergence snapshot:

| Branch ref | `origin/main` ahead | Branch ahead |
|-----------|----------------------|--------------|
| `origin/main` | 0 | 0 |
| `origin/main-iOS` | 212 | 47 |
| `origin/codex/experimental-features` | 76 | 29 |
| `origin/codex/ios-experimental-features` | 123 | 60 |

---

## C. Conservative current interpretation

### PR #8 — `codex/experimental-features` -> `main`

| Item | Status |
|------|--------|
| Branch type | Experimental Apple Watch |
| Affected area | Snorkeling / Apnea / Buddy / exploratory Watch UI and docs |
| Runtime risk | High |
| Documentation risk | Medium |
| Merge recommendation | **Do not auto-merge** |
| Why | Experimental Watch flows must remain isolated from stable Diving MAIN targets and docs |
| Required QA before any merge | macOS build, target-membership review, UX review, safety/legal review, hardware validation |

### PR #9 — `codex/ios-experimental-features` -> `main-iOS`

| Item | Status |
|------|--------|
| Branch type | Experimental iOS companion |
| Affected area | Explore Lab / experimental planner expansions / route-planning UI / related docs |
| Runtime risk | High |
| Documentation risk | Medium |
| Merge recommendation | **Do not auto-merge** |
| Why | Experimental iOS surfaces remain isolated; `main-iOS` itself is now treated as a historical divergent worktree, not a stable MAIN baseline |
| Required QA before any merge | macOS build, manual diff review, sync/security regression check, planner/release review |

---

## D. Merge safety summary

At the documentation/process level, both referenced PRs should still be treated as:

- **unsafe for automatic merge**
- requiring manual conflict resolution
- requiring explicit protection of:
  - Diving MAIN stability
  - visible surface `Start Dive` on stable Watch MAIN
  - Mission Mode safety exclusions and indicator minimalism
  - BUSSOLA terminology
  - inline underwater warning UX
  - compact GPS overlay behavior
  - legal/release/TestFlight docs
  - experimental isolation in `project.yml`

---

## E. Recommended next step

If live PR refresh is required, rerun inspection from an environment with:

- `gh` installed and authenticated, or
- direct access to the GitHub PR pages/API

Until then, keep the standing recommendation: **do not auto-merge PR #8 or PR #9**.
