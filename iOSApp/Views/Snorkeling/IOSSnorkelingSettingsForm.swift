import SwiftUI

/// Backward-compatible wrapper around embeddable Snorkeling settings content.
struct IOSSnorkelingSettingsForm: View {
    var body: some View {
        IOSSnorkelingSettingsContent()
    }
}
