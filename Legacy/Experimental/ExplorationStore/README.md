# Legacy ExplorationStore (archived)

Historical experimental Watch runtime for Apnea/Snorkeling prototyping.

## Status

- **NOT compiled** into MAIN Watch or iOS targets
- **NOT reachable** from production navigation
- **NOT authoritative** for any mathematical or lifecycle behavior on `main`

Production Apnea uses `ApneaSessionEngine` + `ApneaWatchRuntimeStore`.
Production Snorkeling uses `SnorkelingSessionEngine` + dedicated runtime stores.

## Location

`Legacy/Experimental/ExplorationStore/ExplorationStore.swift`

Preserved for historical reference and experimental branch comparison only.
